\newglossaryentry{show}{
	name={show},
	description={ Identity function for the right (as opposed to left) argument. 
		Used to display the a value that has been assigned a name, which by default would not display}
}
